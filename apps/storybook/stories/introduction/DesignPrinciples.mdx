import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Mosaic Design System/Introduction/Design Principles" />

<div className="docs-page">
  <header className="docs-hero">
    <span className="docs-eyebrow">Introduction</span>
    <h1>Design principles</h1>
    <div className="docs-lede">The rules that keep Mosaic coherent as products, teams, locales, and workflows scale.</div>
  </header>

  <div className="docs-card-grid">
    <article className="docs-card"><h3>Clarity over novelty</h3><p>Optimize dense workflows for scanning, comprehension, and confident action.</p></article>
    <article className="docs-card"><h3>Reuse before expansion</h3><p>Compose existing components before proposing a new primitive, variant, or token.</p></article>
    <article className="docs-card"><h3>Accessible by default</h3><p>Keyboard support, semantics, focus, contrast review, and localization are part of the API.</p></article>
    <article className="docs-card"><h3>Bidirectional by design</h3><p>Use logical layout and verify behavior with both LTR and RTL content.</p></article>
    <article className="docs-card"><h3>Truthful documentation</h3><p>Document only shipped behavior. Record uncertainty as a design decision or engineering review.</p></article>
    <article className="docs-card"><h3>Durable foundations</h3><p>Use package tokens and typed APIs instead of copying visual literals from design files.</p></article>
  </div>
</div>
